# 🚀 Быстрый старт Finance Tracker

## 1. Распаковка и установка

```bash
# Распакуйте архив
tar -xzf finance-tracker.tar.gz
cd finance-tracker

# Установите зависимости
npm install
```

## 2. Настройка базы данных (Вариант A: Neon - Рекомендуется)

1. Зарегистрируйтесь на https://neon.tech (бесплатно)
2. Создайте новый проект
3. Скопируйте строку подключения (Connection String)
4. Создайте файл `.env`:

```env
DATABASE_URL="postgresql://user:password@ep-xxx.neon.tech/neondb?sslmode=require"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-here"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

Сгенерируйте секретный ключ:
```bash
openssl rand -base64 32
```

## 2. Настройка базы данных (Вариант B: Supabase)

1. Зарегистрируйтесь на https://supabase.com (бесплатно)
2. Создайте новый проект
3. В Settings → Database скопируйте Connection String (URI format)
4. Создайте файл `.env` как в варианте A

## 3. Инициализация базы данных

```bash
# Создать таблицы
npx prisma migrate dev --name init

# Сгенерировать Prisma Client
npx prisma generate
```

## 4. Запуск приложения

```bash
npm run dev
```

Откройте http://localhost:3000 в браузере!

## 5. Первые шаги

1. Зарегистрируйтесь (создайте аккаунт)
2. Войдите в систему
3. Нажмите "Настроить бюджет"
4. Введите общую сумму (например, 2000 BYN)
5. Категории распределятся автоматически
6. Сохраните и начните добавлять траты!

## 📱 Установка PWA на телефон

### Android (Chrome):
1. Откройте сайт в Chrome
2. Меню (⋮) → "Добавить на главный экран"

### iOS (Safari):
1. Откройте сайт в Safari
2. Кнопка "Поделиться" → "На экран «Домой»"

## 🚀 Деплой на Vercel (бесплатно)

### Вариант 1: Через Git (рекомендуется)

1. Загрузите код на GitHub/GitLab
2. Зайдите на https://vercel.com
3. Нажмите "New Project"
4. Импортируйте ваш репозиторий
5. Добавьте переменные окружения:
   - `DATABASE_URL` - из Neon/Supabase
   - `NEXTAUTH_SECRET` - сгенерированный ключ
   - `NEXTAUTH_URL` - URL вашего приложения на Vercel
   - `NEXT_PUBLIC_APP_URL` - тот же URL
6. Deploy!

После деплоя выполните миграции:
```bash
# Установите переменную DATABASE_URL
export DATABASE_URL="postgresql://..."

# Запустите миграции
npx prisma migrate deploy
```

### Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Деплой
vercel

# Настройте переменные окружения в dashboard
# Продакшн деплой
vercel --prod
```

## ❓ Решение проблем

### Ошибка подключения к БД
- Проверьте `DATABASE_URL` в `.env`
- Убедитесь что строка подключения правильная
- Для Neon добавьте `?sslmode=require` в конец URL

### Ошибка при миграциях
```bash
# Сбросить и пересоздать БД (ВНИМАНИЕ: удалит все данные!)
npx prisma migrate reset

# Или создать новую миграцию
npx prisma migrate dev --name init
```

### npm install выдаёт ошибки
```bash
# Очистите кэш и переустановите
rm -rf node_modules package-lock.json
npm install
```

## 📚 Дополнительно

Полную документацию смотрите в файле `README.md`

## 💬 Нужна помощь?

Проверьте:
1. Правильность `.env` файла
2. Что база данных доступна
3. Что выполнены все миграции
4. Логи в терминале при `npm run dev`

---

**Готово! Теперь вы можете контролировать свои финансы! 💰**
