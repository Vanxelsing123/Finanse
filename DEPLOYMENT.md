# 🚀 Пошаговая инструкция по деплою

Эта инструкция поможет вам развернуть приложение в интернете **бесплатно**.

## Метод 1: Vercel + Neon (Самый простой) ⭐

### Что нам понадобится:
- GitHub аккаунт (бесплатно)
- Vercel аккаунт (бесплатно)
- Neon аккаунт (бесплатно)

### Шаг 1: Загрузите код на GitHub

1. Создайте репозиторий на https://github.com/new
2. Загрузите файлы проекта:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/finance-tracker.git
git push -u origin main
```

### Шаг 2: Создайте базу данных на Neon

1. Перейдите на https://neon.tech
2. Нажмите "Sign up" → войдите через GitHub
3. Нажмите "Create a project"
4. Введите название: `finance-tracker`
5. Выберите регион ближе к вам
6. Нажмите "Create project"
7. **ВАЖНО:** Скопируйте Connection String, он выглядит так:
   ```
   postgresql://username:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
   ```

### Шаг 3: Задеплойте на Vercel

1. Перейдите на https://vercel.com
2. Нажмите "Sign up" → войдите через GitHub
3. Нажмите "Add New..." → "Project"
4. Выберите ваш репозиторий `finance-tracker`
5. В разделе "Environment Variables" добавьте:

   **Переменная 1:**
   - Name: `DATABASE_URL`
   - Value: (вставьте Connection String из Neon)

   **Переменная 2:**
   - Name: `NEXTAUTH_URL`
   - Value: `https://your-app.vercel.app` (замените на ваш URL, его покажет Vercel)

   **Переменная 3:**
   - Name: `NEXTAUTH_SECRET`
   - Value: Сгенерируйте случайную строку (можно на https://generate-secret.vercel.app/)

6. Нажмите "Deploy"

### Шаг 4: Инициализируйте базу данных

После успешного деплоя выполните локально:

```bash
DATABASE_URL="ваш-connection-string-из-neon" npx prisma db push
```

### Шаг 5: Готово! 🎉

Ваше приложение доступно по адресу `https://your-app.vercel.app`

**Обновление приложения:**
Просто делайте `git push` - Vercel автоматически обновит приложение!

---

## Метод 2: Railway (Всё в одном)

### Шаг 1: Railway

1. Перейдите на https://railway.app
2. Войдите через GitHub
3. Нажмите "New Project" → "Deploy from GitHub repo"
4. Выберите ваш репозиторий

### Шаг 2: Добавьте PostgreSQL

1. В проекте нажмите "New" → "Database" → "Add PostgreSQL"
2. Railway автоматически создаст `DATABASE_URL`

### Шаг 3: Добавьте переменные

В настройках проекта → Variables:

- `NEXTAUTH_URL`: `https://your-app.up.railway.app` (замените на ваш URL)
- `NEXTAUTH_SECRET`: случайная строка
- `DATABASE_URL`: уже есть автоматически

### Шаг 4: Деплой

Railway автоматически задеплоит приложение.

После деплоя выполните миграцию:

```bash
DATABASE_URL="url-из-railway" npx prisma db push
```

---

## Проверка работы

После деплоя:

1. Откройте ваш сайт
2. Зарегистрируйтесь
3. Создайте бюджет
4. Добавьте трату

Если всё работает - поздравляю! 🎉

---

## Частые проблемы

### "Internal Server Error" после деплоя

**Причина:** База данных не инициализирована

**Решение:**
```bash
DATABASE_URL="ваш-url" npx prisma db push
```

### "Authentication failed" при входе

**Причина:** Неправильный `NEXTAUTH_SECRET` или `NEXTAUTH_URL`

**Решение:**
- Проверьте что `NEXTAUTH_URL` совпадает с URL сайта
- Сгенерируйте новый `NEXTAUTH_SECRET`

### Приложение работает медленно

**Причина:** Холодный старт на бесплатном тарифе

**Решение:** Это нормально для бесплатных хостингов. При активном использовании будет быстрее.

---

## Обновление приложения

Когда вы вносите изменения в код:

```bash
git add .
git commit -m "Update"
git push
```

Vercel и Railway автоматически обновят приложение!

---

## Дополнительные возможности

### Свой домен

1. В Vercel/Railway перейдите в Settings → Domains
2. Добавьте свой домен
3. Настройте DNS записи у регистратора домена

### Мониторинг

- Vercel: Analytics включена автоматически
- Railway: Metrics доступны в dashboard

---

## Поздравляю! 🎊

Теперь у вас есть работающее приложение в интернете, доступное с любого устройства!
